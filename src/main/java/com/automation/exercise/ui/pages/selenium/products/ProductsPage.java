package com.automation.exercise.ui.pages.selenium.products;

import com.automation.exercise.ui.pages.selenium.base.BasePage;
import org.openqa.selenium.WebDriver;

public class ProductsPage extends BasePage {

    public ProductsPage(WebDriver driver) {
        super(driver);
    }
}
